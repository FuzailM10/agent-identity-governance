# Copy this file to `.env` and fill in real values.
# `.env` is gitignored — real secrets stay on your machine and are NEVER committed.
#   cp .env.example .env

# Postgres password used by the engine + db containers.
POSTGRES_PASSWORD=change-me-locally
